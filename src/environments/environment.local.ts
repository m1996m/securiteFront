import { Environment, EnvironmentType } from './environment.interface';

export const environment: Environment = {
  ENVIRONMENT: EnvironmentType.LOCAL,
  API_URL: 'https://localhost:8000/',
  APP_NAME: 'Local ITechEasyLife',
};
